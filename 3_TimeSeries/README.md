# Assignment 3: Time Series Analysis
**Classifying Cardiac Arrhythmias: A Comparative Analysis**

## 📌 Overview
This assignment tackles the challenge of temporal modeling in healthcare. We engineer features from raw electrocardiogram (ECG) signals and compare sequence classifiers to categorize individual heartbeats into clinically meaningful arrhythmias.

## 📊 Dataset
* **Source:** MIT-BIH Arrhythmia Database (PhysioNet) accessed via the `wfdb` library.
* **Features:** 14 patient records, yielding ~35,000-40,000 labelled beats. 8 engineered features per beat capturing rhythm, morphology, and repolarisation.
* **Target:** AAMI 3-Class Scheme (Normal [N], Supraventricular ectopic [S], Ventricular ectopic [V]).

## 🧠 Methods Applied
Data is structured into sliding windows of $T=10$ consecutive beats. The models are built using **PyTorch**:
1.  **Recurrent Neural Networks (RNN):** A vanilla sequence classifier to understand the vanishing gradient problem.
2.  **Long Short-Term Memory (LSTM):** An extension to evaluate if gating mechanisms improve performance on this sequence length.

## 📈 Evaluation
Models are evaluated on an imbalanced dataset, making Macro F1 and per-class recall (especially for S and V beats) the primary metrics for success, alongside confusion matrices and clinical deployment viability.

## 👥 Team Workflow & Task Distribution
To ensure parallel development and avoid Git merge conflicts, the pipeline is divided into five distinct roles, each with its own dedicated notebook:

1. **Data Explorer (Role 1):** Loads the raw signals via `wfdb`, plots ECG segments, and analyzes the class imbalance.
2. **Signal Engineer (Role 2):** Extracts the 8 mathematical and physical features per heartbeat (e.g., RR intervals, QRS durations).
3. **Data Architect (Role 3):** Maps the target labels to the AAMI scheme, applies feature scaling, and constructs the final PyTorch-ready sliding windows (10-beat sequences).
4. **RNN Specialist (Role 4):** Sets up the PyTorch `Dataset` and `DataLoader`, programs the vanilla RNN architecture, trains the model, and extracts the initial metrics.
5. **LSTM Specialist (Role 5):** Adapts the established training loop for the LSTM extension, evaluates its performance, and generates the final model comparison tables.

## 📂 Project Structure

```text
3_TimeSeries/
│
├── data/
│   ├── 1_raw/                       # Local PhysioNet wfdb records
│   ├── 2_extracted/                 # Raw feature matrices generated by the Signal Engineer (Role 2)
│   └── 3_processed/                 # Scaled 10x8 window tensors generated by the Data Architect (Role 3)
│
├── docs/
│   ├── guide/                       # Assignment instructions
│   └── report/
│       └── Team11_TimeSeries_Report.pdf  # Final scientific report
│
├── figures/
│   ├── exploration/                 # ECG signal plots and class imbalance charts (Role 1)
│   └── evaluation/                  # Loss curves, accuracy plots, and confusion matrices (Roles 4 & 5)
│
├── notebooks/                       # Modular pipeline mapped to team roles
│   ├── 1_DataExploration.ipynb
│   ├── 2_SignalEngineering.ipynb
│   ├── 3_DataArchitecture.ipynb
│   ├── 4_RNN_Modeling.ipynb
│   ├── 5_LSTM_Modeling.ipynb
│   └── Team11_TimeSeries_Notebook.ipynb  # Final unified deliverable running end-to-end
│
├── results/                         # Final evaluation tables: Accuracy, Macro F1, Weighted F1
│
└── README.md                        # This file